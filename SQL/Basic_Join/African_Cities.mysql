select C.NAME
from CITY C 
INNER join COUNTRY CY ON C.COUNTRYCODE = CY.CODE
WHERE CONTINENT = 'Africa'


/* 
select C.NAME
from CITY C 
INNER join 
(SELECT * FROM COUNTRY Y WHERE Y.CONTINENT = 'Africa') AS CY ON C.COUNTRYCODE = CY.CODE 
*/
